'use client';
import LegacyRouteRedirect from 'components/LegacyRouteRedirect';

// Legacy URL: forwarded to the active wallet's equivalent page.
export default function LegacyRedirectPage() {
  return <LegacyRouteRedirect />;
}
