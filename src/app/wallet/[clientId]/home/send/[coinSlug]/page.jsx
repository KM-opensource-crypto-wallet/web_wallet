'use client';
import SendScreen from 'components/Send';

// The coin in the URL is validated and synced to redux by ./layout.jsx.
export default function SendWithCoinSlug() {
  return <SendScreen />;
}
